<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Bulletin - {{ student.fullName }}</title>
    <style>
        @page { margin: 0.5cm; }
        body { font-family: 'DejaVu Sans', sans-serif; font-size: 12px; color: #000; }
        .header { text-align: center; margin-bottom: 20px; border-bottom: 1px solid #000; padding-bottom: 10px; }
        .school-info { width: 100%; margin-bottom: 10px; }
        .student-box { border: 1px solid #000; padding: 10px; margin-bottom: 20px; background-color: #f9f9f9; width: 100%; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th, td { border: 1px solid #000; padding: 6px; text-align: left; }
        th { background-color: #eee; text-align: center; font-weight: bold; }
        .center { text-align: center; }
        .right { text-align: right; }
        .footer-box { width: 32%; display: inline-block; height: 120px; border: 1px solid #000; vertical-align: top; padding: 5px; margin-right: 1%; }
        .avg-row { background-color: #e6f3ff; font-weight: bold; font-size: 14px; }
    </style>
</head>
<body>
    <div class="header">
        <h3 style="margin:0; text-transform:uppercase;">Royaume du Maroc</h3>
        <h5 style="margin:5px 0;">Ministère de l'Éducation Nationale</h5>
        <div style="margin-top:15px; text-align:left;">
            <strong>Établissement:</strong> CampusFlow Academy &nbsp;&nbsp;|&nbsp;&nbsp; 
            <strong>Année:</strong> {{ student.classe.academicYear }}
        </div>
    </div>

    <table class="student-box">
        <tr>
            <td style="border:none;">
                <strong>Nom & Prénom:</strong> {{ student.lastName|upper }} {{ student.firstName }}<br>
                <strong>CNE:</strong> {{ student.cne }}<br>
                <strong>Né(e) le:</strong> {{ student.birthDate|date('d/m/Y') }}
            </td>
            <td style="border:none; text-align:right;">
                <strong>Classe:</strong> {{ student.classe.name }}<br>
                <strong>Niveau:</strong> {{ student.classe.level }}<br>
                <strong>Semestre:</strong> {{ semester }}
            </td>
        </tr>
    </table>

    <table>
        <thead>
            <tr>
                <th width="35%">Matière</th>
                <th width="10%">Coef</th>
                <th width="15%">Note /20</th>
                <th width="15%">Produit</th>
                <th width="25%">Observation</th>
            </tr>
        </thead>
        <tbody>
            {% set totalCoef = 0 %}
            {% set totalPoints = 0 %}
            {% for item in gradesData %}
                {% set produit = item.grade.value * item.subject.coefficient %}
                {% set totalCoef = totalCoef + item.subject.coefficient %}
                {% set totalPoints = totalPoints + produit %}
                <tr>
                    <td>{{ item.subject.name }}</td>
                    <td class="center">{{ item.subject.coefficient }}</td>
                    <td class="center">{{ item.grade.value|number_format(2, ',', ' ') }}</td>
                    <td class="center">{{ produit|number_format(2, ',', ' ') }}</td>
                    <td style="font-style:italic; font-size:10px;">
                        {% if item.grade.value >= 14 %}Bien{% elseif item.grade.value >= 10 %}Passable{% else %}Faible{% endif %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot>
            <tr>
                <td class="right"><strong>TOTAL</strong></td>
                <td class="center"><strong>{{ totalCoef }}</strong></td>
                <td style="background-color:#ccc;"></td>
                <td class="center"><strong>{{ totalPoints|number_format(2, ',', ' ') }}</strong></td>
                <td></td>
            </tr>
            <tr class="avg-row">
                <td colspan="3" class="right">MOYENNE GÉNÉRALE</td>
                <td colspan="2" class="center">{{ (totalPoints / (totalCoef ?: 1))|number_format(2, ',', ' ') }} / 20</td>
            </tr>
        </tfoot>
    </table>

    <div style="margin-top: 30px;">
        <div class="footer-box">
            <strong>Décision du conseil:</strong><br><br>
            {% if (totalPoints / (totalCoef ?: 1)) >= 10 %}Admis{% else %}Redouble{% endif %}
        </div>
        <div class="footer-box">
            <strong>Visa du Tuteur:</strong>
        </div>
        <div class="footer-box">
            <strong>Le Directeur:</strong>
        </div>
    </div>
</body>
</html>