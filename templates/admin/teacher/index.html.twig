{% extends 'base.html.twig' %}

{% block title %}Gestion des Enseignants - CampusFlow{% endblock %}

{% block body %}
<div class="px-4 py-6 sm:px-0">
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold text-gray-900">Gestion des Enseignants</h1>
        <a href="{{ path('admin_teacher_new') }}" class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded">+ Nouvel enseignant</a>
    </div>
    <div class="bg-white shadow overflow-hidden sm:rounded-md">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Nom</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Email</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Spécialité</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Grade</th>
                    <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">Actions</th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                {% for teacher in teachers %}
                <tr>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">{{ teacher.fullName }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm">{{ teacher.user.email }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm">{{ teacher.specialty }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm">{{ teacher.grade }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-right text-sm">
                        <a href="{{ path('admin_teacher_show', {id: teacher.id}) }}" class="text-indigo-600 mr-3">Voir</a>
                        <a href="{{ path('admin_teacher_edit', {id: teacher.id}) }}" class="text-yellow-600 mr-3">Modifier</a>
                        <form method="post" action="{{ path('admin_teacher_delete', {id: teacher.id}) }}" class="inline" onsubmit="return confirm('Supprimer ?');">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teacher.id) }}">
                            <button type="submit" class="text-red-600">Supprimer</button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}

