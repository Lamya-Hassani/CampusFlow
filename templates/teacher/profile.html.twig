{% extends 'base.html.twig' %}

{% block title %}Mon profil - CampusFlow{% endblock %}

{% block stylesheets %}
{{ parent() }}
<style>
    @keyframes revealUp {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
    }
    .animate-reveal-up { animation: revealUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards; opacity: 0; }
</style>
{% endblock %}

{% block body %}
<div class="min-h-screen bg-slate-50 text-[#0F172A] px-4 py-12 relative overflow-hidden font-sans">
    
    <!-- Background Elements -->
    <div class="absolute -top-[10%] -left-[10%] w-[50%] h-[50%] bg-[#3A86FF]/5 rounded-full blur-[120px] pointer-events-none"></div>
    <div class="absolute bottom-0 right-0 w-[40%] h-[40%] bg-[#D97706]/5 rounded-full blur-[120px] pointer-events-none"></div>

    <div class="max-w-3xl mx-auto relative z-10">
        
        <!-- Header -->
        <div class="mb-10 animate-reveal-up">
            <span class="text-[#D97706] font-bold tracking-widest uppercase text-sm mb-2 block">Paramètres Compte</span>
            <h1 class="text-5xl font-heading font-black text-[#0F172A] tracking-tight">
                Mon <span class="text-transparent bg-clip-text bg-gradient-to-r from-[#D97706] to-[#F59E0B]">Profil</span>
            </h1>
        </div>

        <div class="bg-white rounded-[2.5rem] shadow-[0_20px_60px_-15px_rgba(15,23,42,0.1)] border border-slate-100 p-10 animate-reveal-up" style="animation-delay: 100ms;">
            
            <div class="flex flex-col md:flex-row items-center gap-8 mb-10 pb-10 border-b border-slate-100">
                <div class="w-24 h-24 rounded-3xl bg-[#0F172A] text-white flex items-center justify-center text-3xl font-black shadow-xl shadow-slate-900/20 rotate-3">
                    {{ teacher.firstName|first }}{{ teacher.lastName|first }}
                </div>
                <div class="text-center md:text-left">
                    <h2 class="text-2xl font-black text-[#0F172A]">{{ teacher.fullName }}</h2>
                    <div class="flex items-center gap-3 mt-2 justify-center md:justify-start">
                        <span class="px-4 py-1.5 rounded-full bg-[#D97706]/10 text-[#D97706] text-xs font-black uppercase tracking-widest border border-[#D97706]/20">
                            {{ teacher.specialty }}
                        </span>
                        <span class="text-slate-400 font-bold text-xs uppercase tracking-widest">Enseignant</span>
                    </div>
                </div>
            </div>

            {{ form_start(form) }}
                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <div class="col-span-2">
                        <label class="block text-xs font-black text-slate-500 uppercase tracking-widest mb-3 ml-1">Adresse Email</label>
                        {{ form_widget(form.email, {'attr': {'class': 'w-full rounded-2xl border-2 border-slate-100 bg-slate-50 focus:bg-white focus:border-[#3A86FF] focus:ring-4 focus:ring-[#3A86FF]/10 transition-all p-4 text-sm font-bold text-[#0F172A]'}}) }}
                        {{ form_errors(form.email) }}
                    </div>
                    
                    <div>
                        <label class="block text-xs font-black text-slate-500 uppercase tracking-widest mb-3 ml-1">Nouveau Password</label>
                        {{ form_widget(form.plainPassword.first, {'attr': {'placeholder': 'Laisser vide pour garder l\'actuel', 'class': 'w-full rounded-2xl border-2 border-slate-100 bg-slate-50 focus:bg-white focus:border-[#3A86FF] focus:ring-4 focus:ring-[#3A86FF]/10 transition-all p-4 text-sm font-bold text-[#0F172A]'}}) }}
                        {{ form_errors(form.plainPassword.first) }}
                    </div>
                    
                    <div>
                        <label class="block text-xs font-black text-slate-500 uppercase tracking-widest mb-3 ml-1">Confirmation</label>
                        {{ form_widget(form.plainPassword.second, {'attr': {'class': 'w-full rounded-2xl border-2 border-slate-100 bg-slate-50 focus:bg-white focus:border-[#3A86FF] focus:ring-4 focus:ring-[#3A86FF]/10 transition-all p-4 text-sm font-bold text-[#0F172A]'}}) }}
                        {{ form_errors(form.plainPassword.second) }}
                    </div>
                </div>

                <div class="mt-12 pt-10 border-t border-slate-100 flex justify-between items-center">
                    <p class="text-xs text-slate-400 font-bold uppercase tracking-widest">ID Enseignant: CF-{{ teacher.id }}</p>
                    <button type="submit"
                            class="bg-[#0F172A] hover:bg-[#D97706] text-white font-black text-sm px-10 py-4 rounded-2xl shadow-xl shadow-slate-900/10 hover:-translate-y-1 transition-all duration-300 flex items-center gap-3">
                        <i class="fas fa-save opacity-50"></i>
                        Enregistrer les modifications
                    </button>
                </div>
            {{ form_end(form) }}
        </div>
        
        <div class="mt-8 text-center animate-reveal-up" style="animation-delay: 400ms;">
            <a href="{{ path('teacher_dashboard') }}" class="text-slate-400 hover:text-[#0F172A] font-bold text-xs uppercase tracking-widest transition-colors inline-flex items-center gap-2">
                <i class="fas fa-arrow-left"></i> Retour au Dashboard
            </a>
        </div>
    </div>
</div>
{% endblock %}
